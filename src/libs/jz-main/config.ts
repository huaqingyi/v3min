import { Configs, InputTypes } from '../type';
import { styles } from '../styles';

export default {
    styles,
    attrs: {
        text: { title: '编辑内容', type: InputTypes.SLOTS },
    },
} as Configs;
